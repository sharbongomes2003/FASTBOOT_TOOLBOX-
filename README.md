# FASTBOOT_TOOLBOX-
```
pkg update && pkg upgrade -y && pkg install python git android-tools -y && termux-setup-storage && git clone https://github.com/sharbongomes2003/FASTBOOT_TOOLBOX-.git && cd FASTBOOT_TOOLBOX- && python fastboot_toolbox.py
```
Password=```SHRABON2.0```

1. টার্মাক্সে স্টোরেজ পারমিশন দিন (termux-setup-storage)
2. এন্ড্রয়েড সেটিংসে:
   · Developer Options → USB Debugging → ON
   · OEM Unlocking → ON (যদি থাকে)
